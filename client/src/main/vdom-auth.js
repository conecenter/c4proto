
import React from 'react'

export default function VDomAuth(feedback,localStore){
/*
    const ChangePassword = React.createClass({
        render: function () {
            const onChange = event => this.setState({value: event.target.value});
            React.createElement("div",{},[
                React.createElement("input",{onChange:handleChange("0"),value},null),
                React.createElement("input",{onChange:handleChange("1"),value},null)
            ])
        }
    });

    const transforms= {
        tp: {
            SignIn,ChangePassword
        }
    };
    return ({transforms});*/
}