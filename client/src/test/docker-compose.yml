services:
  node:
    command: ["sh","-c","npm install express && node src/test/server.js"]
    image: node:14.3.0
    volumes:
    - ../../src:/app/src
    - ../../vendor:/app/vendor
    working_dir: /app
    ports:
    - "127.0.0.10:80:3000"
    environment:
      HTTP_PORT: 3000
version: '2'
